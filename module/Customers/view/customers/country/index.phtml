<?php
$this->headTitle('Countries');

$this->mainMenu()->setActiveItemId('countries');

$this->pageBreadcrumbs()->setItems([
            'Home'=>$this->url('home'),
            'Manage Countries'=>$this->url('beheer/countries'),
            ]);
?>

<h1>Manage Countries</h1>

<p>
<a class="btn btn-primary" href="
    <?= $this->url('beheer/countries', ['action'=>'add']); ?>">
    <span class="glyphicon glyphicon-plus" aria-hidden="true"></span> New Country
</a>
</p>

<table class="table table-striped">

   <tr>
        <th>Name</th>
        <th>Date Created</th>
    </tr>
    
    <?php foreach ($countries as $country): ?>
    
    <tr>
        <td>
            <?= $this->escapeHtml($country->getName()); ?></a> 
        </td>
        <td><nobr><?= (is_object($user->getDateCreated()) ? $this->escapeHtml($user->getDateCreated()->format('Y-m-d')): ''); ?></nobr></td>        
        <td>
            <a class="btn btn-info" href="<?= $this->url('beheer/countries', 
                    ['action'=>'edit', 'id'=>$country->getId()]); ?>">
                <span class="glyphicon glyphicon-pencil" ></span> Edit
            </a>
        </td>    
    </tr>
        
    <?php endforeach; ?>   
    
</table>
